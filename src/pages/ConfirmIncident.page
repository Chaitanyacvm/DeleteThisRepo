<apex:page standardController="FM_Incident__c" extensions="FM_ConfirmIncident" tabStyle="FM_Incident__c" action="{!confirmIncident}">
    <apex:form > 
        <apex:pageBlock title="Confirm Incident">
            <apex:pageMessages ></apex:pageMessages>
            <apex:pageBlockButtons location="bottom">
                <apex:commandButton value="back" action="{!back}"/>
            </apex:pageBlockButtons>
        </apex:pageBlock>
    </apex:form>
</apex:page>